use crate::app::App;
use ratatui::Frame;

/// Render the full UI from the current application state.
/// This function must not mutate `app`.
pub fn render(frame: &mut Frame, app: &App) {
    todo!()
}
